l=[]
k=[]
if __name__ == '__main__':
    for _ in range(int(input())):
        name=input()
        score=float(input())
        l+=[[name,score]]
        k+=[score]
    b=sorted(list(set(k)))[1]
    for a,c in sorted(l):
        if c==b:
               print(a)
